#오픈소스 소프트웨어
-한 학기동안 고생 많으셨습니다.
$ git tag v1.0
$ git branch master
$ git branch feature
-재미있는 수업이었기를 바랍니다.
$ git tag v2.0
$ git branch feature -u
-건강한 모습으로 다시 뵙겠습니다.
$ git tag v3.0
-야호, 여름방학이다~!
$ git branch master
$ git checkout -b <feature>

$ git squash master
$ git tag "final"

